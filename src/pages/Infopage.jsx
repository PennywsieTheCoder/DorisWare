const content = {
  delivery: ["Delivery information", "Orders are prepared after payment confirmation. Delivery timing and fees will be confirmed at checkout once delivery zones are connected to the backend.", ["Delivery estimates", "Delivery address changes", "Tracking updates"]],
  returns: ["Returns & refunds", "If something arrives damaged or is not what you expected, contact our support team with your order reference. The final returns workflow will be activated with order management.", ["Return eligibility", "Damaged items", "Refund timing"]],
  privacy: ["Privacy policy", "We only use customer information to process orders, provide support, and improve DorisWare. A final legal policy should be reviewed before launch.", ["Information we collect", "How we use it", "Your choices"]],
  terms: ["Terms of service", "Using DorisWare means you agree to provide accurate order and payment details. Final terms should be reviewed before accepting live orders.", ["Orders and availability", "Pricing", "Customer responsibilities"]],
};
export default function InfoPage({ type }) { const [title, intro, items] = content[type] ?? content.delivery; return <div className="mx-auto min-h-[60vh] max-w-3xl px-6 py-16"><p className="text-sm font-semibold uppercase tracking-wider text-green-700 dark:text-green-400">DorisWare support</p><h1 className="mt-3 font-serif text-4xl font-semibold text-stone-900 dark:text-stone-100">{title}</h1><p className="mt-6 text-lg leading-8 text-stone-600 dark:text-stone-300">{intro}</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{items.map(item => <div key={item} className="rounded-2xl border border-stone-200 p-5 font-medium text-stone-800 dark:border-stone-700 dark:text-stone-200">{item}</div>)}</div></div>; }
